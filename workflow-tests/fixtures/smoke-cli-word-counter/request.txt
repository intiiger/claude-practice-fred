텍스트 파일의 단어 빈도를 세는 Python CLI 도구를 만들어줘. 입력으로 파일 경로를 받고, --top N 옵션으로 빈도 상위 N개 단어를 출력한다. 대소문자는 구분하지 않고, 구두점은 제거한다. N을 지정하지 않으면 전체를 빈도 내림차순으로 출력한다. 엔트리포인트는 `python -m word_counter <파일경로>` 형태로 실행할 수 있도록 `src/word_counter/__main__.py` 에 둔다.
